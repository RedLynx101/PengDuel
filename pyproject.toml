[tool.poetry]
name = "repl-nix-pengduel"
version = "0.1.0"
description = "A local browser physics game built with Flask, JavaScript, and HTML Canvas."
authors = ["Noah Hicks"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
flask = "^3.1.3"
gunicorn = "^26.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
